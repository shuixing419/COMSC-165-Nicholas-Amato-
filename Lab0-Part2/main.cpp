#include <iostream>

using namespace std;

int main()
{
    float area, base, height;
    base = 3.0;
    height = 4.0;

    // Compute the area of the triangle and output it
    area = (base * height) / 2.0;
    cout << "The area of the triangle is: " << area << endl;

    return 0;
}
